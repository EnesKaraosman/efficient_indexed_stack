enum IndexCalculationStrategy {
  /// Calculates the indices around the current index
  aroundCurrentIndex,

  /// Calculates the indices before the current index
  beforeCurrentIndex,

  /// Calculates the indices after the current index
  afterCurrentIndex,
}
